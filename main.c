#include <stdio.h>
int main()
{
int a,b,c,d,f,e;
scanf("%d %d %d %d %d %d", &a, &b, &c, &d, &f, &e);
printf("<<%d + %d + %d + %d + %d + %d = %d>>", a, b, c, d, f, e, a + b + c + d + f + e);
return 0;
}
/*
#include <stdio.h>
#include <windows.h>

int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    printf("«Пора идти отдыхать!»\n");
    return 0;
}

#include <stdio.h>
*/
